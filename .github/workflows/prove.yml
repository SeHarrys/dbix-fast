name: CI
run-name: ${{ github.actor }} CI GitHub Actions 🚀

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    uses: SeHarrys/dbix-fast/.github/workflows/cpan-test.yml@master